# Changelog

This is the monorepo index. Per-package changelogs live in each `packages/*/CHANGELOG.md` and are managed by Changesets.

## 0.1.0 — 2025-XX-XX

- Initial `@klank/sdk` release (pre-refresh baseline). See `packages/sdk/CHANGELOG.md` for the authoritative history once the package is scaffolded.
