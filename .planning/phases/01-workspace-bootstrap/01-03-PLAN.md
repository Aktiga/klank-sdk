---
phase: 01-workspace-bootstrap
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - examples/echo-bot-ts/package.json
  - examples/echo-bot-ts/tsconfig.json
  - examples/ci-bot-ts/package.json
  - examples/ci-bot-ts/tsconfig.json
  - examples/webhook-bot/package.json
  - examples/webhook-bot/tsconfig.json
  - packages/create-bot/package.json
  - packages/create-bot/tsconfig.json
  - tsconfig.json
  - .planning/PROJECT.md
autonomous: true
requirements: [HOUSE-02, HOUSE-03]
must_haves:
  truths:
    - "Every TS example is a private workspace package depending on @klank/sdk via workspace:*"
    - "examples/echo-bot-rust is excluded from the pnpm workspace"
    - "packages/create-bot has engines.node>=20 and is type-checked by tsc -b"
    - "PROJECT.md no longer claims Node 18+ support"
    - "Root tsc -b solution file references all TS packages"
  artifacts:
    - path: "examples/echo-bot-ts/package.json"
      provides: "Private example with workspace SDK dep"
      contains: "\"@klank/sdk\": \"workspace:*\""
    - path: "tsconfig.json"
      provides: "Root solution referencing sdk + create-bot + TS examples"
  key_links:
    - from: "examples/*/tsconfig.json"
      to: "packages/sdk"
      via: "project reference"
      pattern: "packages/sdk"
---

<objective>
Wave 3 — Register examples and `packages/create-bot` as first-class
workspace members so they type-check against the real `@klank/sdk` source,
and update PROJECT.md to declare Node 20 (per D2).

Purpose: HOUSE-02 coverage for in-tree consumers, HOUSE-03 full project-
reference graph, D2 docs hygiene. Prepares the workspace for CI wiring in
plan 01-04.
Output: `pnpm -w build` still green after adding every TS package to the
reference graph.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-workspace-bootstrap/01-CONTEXT.md
@.planning/phases/01-workspace-bootstrap/01-RESEARCH.md
@.planning/phases/01-workspace-bootstrap/01-02-SUMMARY.md
@packages/create-bot/package.json
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register TS examples as private workspace packages</name>
  <read_first>
    - examples/echo-bot-ts/index.ts (confirm existing imports)
    - examples/ci-bot-ts/index.ts
    - examples/webhook-bot/index.ts
    - .planning/phases/01-workspace-bootstrap/01-RESEARCH.md §9 (examples as workspace members)
  </read_first>
  <action>
For each of the three TS example directories (`examples/echo-bot-ts`, `examples/ci-bot-ts`, `examples/webhook-bot`):

1. Create `package.json` with exactly (substituting `<dir-name>` per example):

```json
{
  "name": "@klank-examples/<dir-name>",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@klank/sdk": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
```

2. Create `tsconfig.json` in each example dir with exactly:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "dist",
    "noEmit": true,
    "composite": true
  },
  "include": ["*.ts", "src/**/*.ts"],
  "references": [
    { "path": "../../packages/sdk" }
  ]
}
```

3. Verify no example imports from a relative path into `packages/sdk/src/`. If any example still does, rewrite it to `import { ... } from '@klank/sdk'`.

4. Confirm `examples/echo-bot-rust` is untouched (no `package.json` created, no `tsconfig.json` created).

5. Run `pnpm install` so the workspace picks up the new members and links `@klank/sdk` into each example's node_modules.
  </action>
  <verify>
    <automated>for d in echo-bot-ts ci-bot-ts webhook-bot; do test -f "examples/$d/package.json" || exit 1; test -f "examples/$d/tsconfig.json" || exit 1; grep -q '"@klank/sdk": "workspace:\*"' "examples/$d/package.json" || exit 1; grep -q '"private": true' "examples/$d/package.json" || exit 1; done && ! test -f examples/echo-bot-rust/package.json && pnpm install --frozen-lockfile</automated>
  </verify>
  <acceptance_criteria>
    - `examples/echo-bot-ts/package.json` exists, is private, has `@klank/sdk: workspace:*`
    - `examples/ci-bot-ts/package.json` exists, is private, has `@klank/sdk: workspace:*`
    - `examples/webhook-bot/package.json` exists, is private, has `@klank/sdk: workspace:*`
    - Each example has a `tsconfig.json` with a reference to `../../packages/sdk`
    - `examples/echo-bot-rust/package.json` does NOT exist
    - `pnpm install` succeeds and updates `pnpm-lock.yaml` (or no changes if already linked)
  </acceptance_criteria>
  <done>Three TS examples are workspace packages, Rust example excluded.</done>
</task>

<task type="auto">
  <name>Task 2: Add create-bot to project references; update PROJECT.md Node version</name>
  <read_first>
    - packages/create-bot/package.json (baseline — determine existing scripts, deps)
    - .planning/PROJECT.md (the "Node 18+" line to update)
    - .planning/phases/01-workspace-bootstrap/01-CONTEXT.md (D2)
  </read_first>
  <action>
1. Update `packages/create-bot/package.json` to add/ensure:
   - `"engines": { "node": ">=20" }`
   - `"private": true` if not already set (it is NOT in HOUSE scope for publishing per CONTEXT — "attw packages: register @klank/sdk only. packages/create-bot/ is not yet a publishable entry"). Mark it private for Phase 1.
   - `"type": "module"` (align with workspace ESM stance)
   - A `"typecheck": "tsc -b"` script

   Do NOT change its `name` field. Do NOT add it as a dependency of anything. Leave source files alone.

2. Create or overwrite `packages/create-bot/tsconfig.json` with exactly:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    "noEmit": true,
    "composite": true
  },
  "include": ["src/**/*"]
}
```

If `packages/create-bot/src/` does not exist yet, create it as an empty directory with a single `packages/create-bot/src/index.ts` containing exactly:

```ts
// Placeholder — full scaffolder lands in Phase 9 (PG-04).
export {}
```

3. Update root `tsconfig.json` to reference every TS package. Overwrite with exactly:

```json
{
  "files": [],
  "references": [
    { "path": "./packages/sdk" },
    { "path": "./packages/create-bot" },
    { "path": "./examples/echo-bot-ts" },
    { "path": "./examples/ci-bot-ts" },
    { "path": "./examples/webhook-bot" }
  ]
}
```

4. Update `.planning/PROJECT.md`: locate the tech-stack line mentioning "Node 18+" (or "Node 18" / "node18") and change it to "Node 20+". Do a single targeted edit — do not restructure the document.

```bash
grep -n 'Node 18' .planning/PROJECT.md
```

Replace each occurrence with `Node 20`.

5. Verify end-to-end:

```bash
pnpm install --frozen-lockfile
pnpm -w build
pnpm --filter @klank/sdk test
```

All must succeed. `tsc -b` now walks every reference and reports zero errors.
  </action>
  <verify>
    <automated>grep -q '"node": ">=20"' packages/create-bot/package.json && test -f packages/create-bot/tsconfig.json && test -f packages/create-bot/src/index.ts && grep -q './packages/create-bot' tsconfig.json && grep -q './examples/echo-bot-ts' tsconfig.json && ! grep -q 'Node 18' .planning/PROJECT.md && pnpm -w build && pnpm --filter @klank/sdk test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"node": ">=20"' packages/create-bot/package.json`
    - `test -f packages/create-bot/tsconfig.json`
    - `grep -q './packages/create-bot' tsconfig.json`
    - `grep -q './examples/echo-bot-ts' tsconfig.json`
    - `grep -q './examples/ci-bot-ts' tsconfig.json`
    - `grep -q './examples/webhook-bot' tsconfig.json`
    - `! grep -q 'Node 18' .planning/PROJECT.md`
    - `pnpm -w build` exits 0
    - `pnpm --filter @klank/sdk test` exits 0
  </acceptance_criteria>
  <done>Full project-reference graph compiles; PROJECT.md reflects Node 20; smoke test still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new runtime surface. This plan only adds workspace manifests and tsconfigs. Inherits boundaries from plans 01-01 / 01-02.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Tampering | Example package.json transitive deps | mitigate | Examples declare only `@klank/sdk` (workspace) + `typescript` (already in root lockfile). No new registry surface. |
| T-1-02 | Tampering | n/a | — | No workflow files touched. |
| T-1-03 | Elevation | Accidental publish of example packages | mitigate | All examples marked `"private": true` — pnpm/npm refuse to publish. |
</threat_model>

<verification>
```
pnpm install --frozen-lockfile
pnpm -w build
pnpm --filter @klank/sdk test
grep -n 'Node 20' .planning/PROJECT.md
```
</verification>

<success_criteria>
- Three TS examples are workspace members with `@klank/sdk: workspace:*`
- Rust example still excluded
- `packages/create-bot` type-checks via `tsc -b` with `engines.node>=20` and `private: true`
- Root solution file references all five TS packages
- PROJECT.md declares Node 20
- Full workspace build + test green
</success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-bootstrap/01-03-SUMMARY.md`.
</output>
